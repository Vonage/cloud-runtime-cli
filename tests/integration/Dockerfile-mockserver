FROM alpine:3.20

RUN apk add --no-cache bash

WORKDIR /app

# Copy pre-built binary from CI
COPY bin/mockserver /app/mockserver
COPY scripts/run_mockserver.sh /app/run_mockserver.sh

RUN chmod +x /app/mockserver /app/run_mockserver.sh

CMD ["/app/run_mockserver.sh"]
